outputs
processed_elev_attributes

# Large/regenerable data & benchmark trees (mirrors the existing `outputs`
# exclusion above -- these are archived separately, not via git).
processed_data/
benchmark/

# Python bytecode/caches
__pycache__/
*.pyc
.pytest_cache/

# Editor-local settings
.vscode/

# Zenodo-archive-only README (frozen snapshot doc for the data deposit,
# not the GitHub-facing README) -- kept locally, not in this repo.
README_2.md